# User preferences

User is called Mario (Johannes Mario Meissner), lives in Japan, prefers JST timezone for timestamps.
Fetch latest master/main/branches when exploring local code, avoid referrerring to stale local repos until refreshed.

Use the `minions` skill proactively whenever a task splits into parallelizable bounded
chunks, and always when Mario says "minions". Always review/double-check minion work.

## This machine

@CLAUDE.local.md
