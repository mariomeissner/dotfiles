# Agent Instructions

- Always read README.md and maintain it / update it whenever relevant.
- Do not run Homebrew commands such as `brew`, `brew bundle`, or `brew update`. Ask the user to run them instead.
- After making changes, proactively support the user in committing. Confirm git status, confirm with user, and commit when ready.